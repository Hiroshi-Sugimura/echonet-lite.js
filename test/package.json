{
  "name": "echonet-lite-test",
  "version": "1.0.0",
  "description": "Tests for echonet-lite.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "../index.js"
    ],
    "testMatch": [
      "**/*.test.js"
    ]
  },
  "dependencies": {
    "date-utils": "^1.2.21"
  }
}